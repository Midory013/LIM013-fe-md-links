#!/usr/bin/env node


//const mdlinks = require('./md-links.js')
//console.log(mdlinks);
//Pasando argumentos de linea de comandos en Nodejs
//como usar argumentos a traves de la forma incorporada(process.argv)
//
const proceses = process.argv.slice(2);//guarda un array de elementos, el primer elemto la ruta del ejecuta y la seg que archivo le paso 
console.log(proceses);
/*
const  opcitiStats = (arrLinks) => {
    const statsLinks = arrLinks.length;
    
}*/
/*
if (process.length === 1) {
    console.log(mdlinks(proceses[0]));
} */  

